Need to fix;

verify chunks of Mmap are being filled? Seems like they arent on KV init because the file has no header data in it

Second insertion Node btype = 0, which shouldnt be possible?

Some weird issue where running the same executable is giving different outputs ?
