{
    "version": "0.2.0",
    "configurations": [

        {
            "name": "CMake Debug (LLDB)",
            "type": "lldb",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "CMake: Build",
        }
    ]
}